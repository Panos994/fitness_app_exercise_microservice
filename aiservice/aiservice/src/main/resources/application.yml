server:
  port: 8085
spring:
  application:
    name: ai-service
  data:
    mongodb:
      host: localhost       # <--- ??? ???????
      port: 27017           # <--- ??? ???????
      database: fitnessrecommendation # <--- ??????? ?? ????? ?????

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8799/eureka/
  instance:
    # 1. ????????? ?? service ?? ??????????? ?? IP ???? ??? hostname
    prefer-ip-address: true
    # 2. ???????????? ???? ?? localhost (127.0.0.1) ?? IP
    ip-address: 127.0.0.1

   #for rabbitmq : open docker desktop and start the container
   #Or cmd >: docker run -it --rm --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4-management
